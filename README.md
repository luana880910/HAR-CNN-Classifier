# HAR-CNN-Classifier
Homework

## 使用方法
先打開期末專題報告README

再打開主要介紹

如果點開程式碼出現亂碼，可以點開程式碼看看，那邊會有完整程式碼介紹。
 
如果想看看不同的model就可以直接把test的load_model改掉。

avg是最後面才加上去的，其餘都是用最簡單的超級overfitting版本。
分為改先改動epoch再改動drop的值。

## 執行方法

在CMD打 python train (訓練)或是 python tesst (測試)。

想測試不同的model，可以直接進入test檔中改.h5檔的名稱。
